# The badge fetches this from the browser; the runtime route uses BACKEND_URL server-side.
NEXT_PUBLIC_BACKEND_URL=http://localhost:8001
BACKEND_URL=http://localhost:8001
# CopilotKit posts telemetry and fetches CDN product announcements unless disabled.
COPILOTKIT_TELEMETRY_DISABLED=true
