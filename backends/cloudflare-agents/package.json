{
  "name": "backend-cloudflare-agents",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port ${PORT:-8002}",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "types": "wrangler types"
  },
  "dependencies": {
    "@ag-ui/core": "^0.0.58",
    "@ag-ui/encoder": "^0.0.58",
    "@ai-sdk/provider": "^4.0.7",
    "agents": "^0.20.1",
    "ai": "^7",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "wrangler": "^4.123.0"
  }
}
